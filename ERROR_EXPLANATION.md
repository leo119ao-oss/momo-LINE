# エラーの原因と修正について（わかりやすい説明）

## エラーが起きた場所
`src/app/api/auth/token/route.ts` というファイルの29行目で、ビルド（プログラムを実行可能な形に変換すること）時にエラーが発生しました。

## エラーの内容
```
Type error: Property 'catch' does not exist on type 'PromiseLike<void>'.
```
日本語にすると：
「`PromiseLike<void>`という型には、`catch`という機能が存在しません」

## わかりやすく説明すると

### 1. プログラムの「型」とは？
プログラムには「型」という概念があります。これは、データがどんな種類のものかを示すラベルのようなものです。

例：
- 「文字列型」→ 文字の並び（例：「こんにちは」）
- 「数値型」→ 数字（例：123）
- 「約束型（Promise）」→ 「後で結果を返します」という約束

### 2. 今回の問題
今回のエラーは、以下のような状況でした：

**問題のコード：**
```typescript
supabaseAdmin
  .from('tokens')
  .delete()
  .lt('expires_at', new Date().toISOString())
  .then(() => {})  // 「成功したらこれをする」
  .catch((err) => {}); // 「エラーが起きたらこれをする」← ここでエラー
```

**なぜエラーになったか：**
- `delete()`という操作が返すのは「`PromiseLike<void>`」という型
- これは「約束に似たもの」だが、完全な「約束（Promise）」ではない
- `.catch()`は完全な「約束（Promise）」にしか使えない
- だから「`catch`が存在しません」というエラーが出た

### 3. 修正方法
`Promise.resolve()`という機能を使って、「約束に似たもの」を「完全な約束」に変換しました。

**修正後のコード：**
```typescript
Promise.resolve(  // 「約束に似たもの」を「完全な約束」に変換
  supabaseAdmin
    .from('tokens')
    .delete()
    .lt('expires_at', new Date().toISOString())
)
  .then(() => {})  // 「成功したらこれをする」
  .catch((err) => {}); // 「エラーが起きたらこれをする」← これで動く！
```

## 実際に何をしていたか
このコードは、**期限切れのトークン（アクセス用の鍵）を削除する**処理をしていました。

- トークンは1日間有効
- 期限切れのトークンは自動的に削除する必要がある
- この削除処理は、メインの処理（新しいトークンの生成）を邪魔しないように、バックグラウンドで実行される
- もし削除処理でエラーが起きても、メインの処理には影響しないようにする必要がある

そのため、`.catch()`でエラーをキャッチ（捕まえる）必要がありました。

## まとめ
- **問題**：Supabaseの`delete()`が返す型が「約束に似たもの」で、`.catch()`が使えなかった
- **解決**：`Promise.resolve()`で「完全な約束」に変換して、`.catch()`が使えるようにした
- **結果**：ビルドが成功し、期限切れトークンの削除処理が正常に動作するようになった

## 技術的な詳細（参考）
- `PromiseLike<T>`: 非同期処理の結果を表すが、`Promise`の完全な機能を持たない型
- `Promise<T>`: 非同期処理の結果を表し、`.then()`や`.catch()`などのメソッドを持つ完全な型
- `Promise.resolve()`: 値を`Promise`に変換する関数

